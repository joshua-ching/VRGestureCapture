using System.Collections;
using System.Collections.Generic;
using UnityEditor.Experimental.GraphView;
using UnityEngine;
using UnityEngine.Scripting.APIUpdating;

public class PlayerMovement : MonoBehaviour
{

    //dead script

    // public Rigidbody rb;

    // public InputData inp;

    // Vector3 moveDirection;

    // public float moveSpeed;
    // void Start()
    // {
    //     rb = GetComponent<Rigidbody>();
    // }

    void Update()
    {
        // rb.position += new Vector3(inp.stickL.x * Time.deltaTime * moveSpeed,0,inp.stickL.y * Time.deltaTime * moveSpeed);

        // moveDirection = transform.forward * inp.stickL.y + transform.right * inp.stickL.x;
        // rb.AddForce(moveDirection * Time.deltaTime * moveSpeed);
    }
}

